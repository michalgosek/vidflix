mysql:
  dataSource:
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: doom2020
    jdbcUrl: jdbc:mysql://localhost:3306/mydb133?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    maximumPoolSize: 10

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties.hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
